#ifndef __UTILS_BOOLEAN_H
#define __UTILS_BOOLEAN_H
/*
 * This header file defines the Bool enum.
 * Note that TRUE == 1 and FALSE == 0 for convenience (in "if" statements for example).
 */

typedef enum {
	FALSE = 0, TRUE = 1
} Bool;

#endif